<header>
  <nav class="navbar navbar-default navbar-static-top">
    <div class="container">
      <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-left" href="/"><img src="/images/logo.png" alt="Youniversity"></a>
      </div>
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        
      
         <% if logged_in? %>
      <ul class="nav navbar-nav">
              <li><%= form_tag articles_path, :method => 'get', class: 'navbar-form', role: 'search' do %> <%#　projects_pathはモデル名で読みかえること %>
                       
                        <form class="navbar-form" >
                           <div class="form-group">
                              <%= text_field_tag :search, params[:search] %>
                              <%#= text_field_tag :keyword, @keyword, class: 'form-control input-lg', placeholder: "キーワードを入力"  %>
                            </div>
                            <%= submit_tag "検索", :keyword => nil %>
                            
                            <%##params[:search]%>
                        </form>  
                      <% end %>
              </li>   
          <li class="dropdown active" >
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">カテゴリー<span class="caret"></span></a>
                  <ul class="dropdown-menu">
                    <% Category.all.each do |category| %>
                      <li><%= link_to category.category_name, categories_path(category_id: category.id) %></li>
                     <%# <li><%= link_to category.category_name, root_path(category_id: category.id)</li>%>
                    <% end %>
                    
                  </ul>
              </li>
      		</li>
          
      </ul>
      
              <ul class="nav navbar-nav navbar-right"> 
                <li class="dropdown active" >
                  <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">マイページ<span class="caret"></span></a>
                      <ul class="dropdown-menu">
                        <li><%= link_to 'マイページ', user_path(current_user) %></li>
                         
                        <li><%= link_to 'Logout', logout_path, method: :delete %></li>
                        <li><%= link_to '退会', @user, method: :delete, data: { confirm: '本当に退会しますか？' } %></li>
                      </ul>
              </ul>
          
          
        <% else %>
        <ul class="nav navbar-nav navbar-nav">
         
            <li><%= form_tag articles_path, :method => 'get', class: 'navbar-form', role: 'search' do %> <%#　projects_pathはモデル名で読みかえること %>
                 
                  <form class="navbar-form" >
                     <div class="form-group">
                        <%= text_field_tag :search, params[:search] %>
                        <%#= text_field_tag :keyword, @keyword, class: 'form-control input-lg', placeholder: "キーワードを入力"  %>
                      </div>
                      <%= submit_tag "検索", :keyword => nil %>
                      
                      <%##params[:search]%>
                  </form>  
                <% end %>
            </li>   
            <li class="dropdown active" >
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">カテゴリー<span class="caret"></span></a>
                  <ul class="dropdown-menu">
                    <% Category.all.each do |category| %>
                      <li><%= link_to category.category_name, categories_path(category_id: category.id) %></li>
                     <%# <li><%= link_to category.category_name, root_path(category_id: category.id)</li>%>
                    <% end %>
                    
                  </ul>
              </li>
      		</li>
        </ul>
       
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav navbar-right"> 
            <li><%= link_to '新規登録', signup_path %></li>
            <li><%= link_to 'ログイン', login_path %></li>
            
                
             
          </ul>  
          <% end %>  
          </div>
      </ul>
      </div>
    </div>
  </nav>
</header>