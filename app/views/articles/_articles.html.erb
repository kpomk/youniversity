<ul class="media-list">
  <% articles.each do |article| %>
    <% user = article.user %>
    <div class="media-body">
        <div>
          <span class="text-muted"><%= article.created_at %></span>
        </div>
        <div>
          <p><%= article.title %></p>
        </div>
      </div>
    </li>
  <% end %>
  <%#= paginate articles %>
</ul>